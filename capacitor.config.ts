import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.Qrsystem',
  appName: 'Qrsystem',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
